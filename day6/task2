import csv

with open("student.csv", "r") as file:
    reader = csv.DictReader(file)
    for row in reader:
        if row["Grade"] != "F":
            print(f"{row['Name']},Grade: {row['Grade']} has passed the course.")

   
